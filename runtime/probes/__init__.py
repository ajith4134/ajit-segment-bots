"""The substrate's own probes.

RL-069: the runtime substrate is off-diagram -- it gets its own status-board tile,
never a cell on the part monitor. This package is how the substrate measures
itself; see substrate_probes.py.
"""
