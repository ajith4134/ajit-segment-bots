"""The bot's features. One module per part, named for the part it is.

Every module here carries a literal PART_DECLARATION whose consumes and produces
equal the blueprint's (RL-067), read without importing it (RL-070).
"""
