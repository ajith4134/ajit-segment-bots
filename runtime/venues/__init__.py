"""One module per venue, and the shape they all are.

Nothing outside this package names a venue. A part takes an adapter and the set
of adapters comes from settings (spec section 3.1), so adding a third venue is a
new module here plus one settings line -- never an edit to a part.
"""
